{
  "name": "server",
  "version": "1.0.0",
  "description": "this is the backend of the backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js"
  },
  "author": "mahesh",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.6.1"
  }
}
